name: Validate and Push Nginx files to Development

on:
  push:
    branches:
      - '**'   

  pull_request:
    branches:
      - develop

jobs:
  validate:
    runs-on: robot-shop-runner-azure-dev

  deploy:
    runs-on: robot-shop-runner-azure-dev